# Historical artifacts

`receipts-v0.1.0.jsonl` is the original sample log shipped before the v0.2
repair. It remains unchanged so earlier claims can still be inspected.

The v0.1 profile used a private receipt envelope and the earlier green/amber/red
vocabulary. It must not be appended to, promoted as Wire Canon conformance, or
verified as a v0.2 chain.

